# Copy to .env.local for local development against the FastAPI backend:
#   cp .env.local.example .env.local
#
# Leave NEXT_PUBLIC_API_BASE unset (the Vercel default) and the app serves the
# built-in demo routes instead — a snapshot of a real pipeline run, no backend.
NEXT_PUBLIC_API_BASE=http://localhost:8000
